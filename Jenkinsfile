pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo "hello from build from github, do nothing" 
                sh ' bash script.sh'
            }
        }
      

        
    }
}
