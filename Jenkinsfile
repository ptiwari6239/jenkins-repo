pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo "hello from build from github, do nothing, jai shree ram " 
                sh ' bash script.sh'
            }
        }
         
    }
}
